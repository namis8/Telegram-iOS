workflows:
  telegram-ios-build:
    name: Build Telegram 11.8.1 Unsigned
    instance_type: mac_mini_m2
    environment:
      groups:
        - telegram
      xcode: latest
    scripts:
      - name: Generate Project and Build
        script: |
          set -e
          git submodule update --init --recursive
          mkdir -p build-system/local-config
          echo "{}" > build-system/local-config/config.json
          python3 build-system/Make/Make.py --cacheDir="$HOME/telegram-cache" generateProject --configurationPath=build-system/local-config/config.json --xcodeManagedCodesigning --disableProvisioningProfiles
          xcodebuild build \
            -workspace Telegram-iOS.xcworkspace \
            -scheme Telegram \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO
          mkdir -p Payload
          mv build/Release-iphoneos/Telegram.app Payload/
          zip -qr Telegram_Unsigned_11.8.1.ipa Payload
    artifacts:
      - Telegram_Unsigned_11.8.1.ipa
